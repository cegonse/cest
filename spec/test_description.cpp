#include <cest>


describe("a Cest test suite", []() {
    beforeAll([]() { });

    afterAll([]() { });

    beforeEach([]() { });

    afterEach([]() { });

    it("can contain test cases", []() { });

    it("can also have test cases with unicode symbols 😀", []() { });

    xit("can be skipped", []() { });
});
